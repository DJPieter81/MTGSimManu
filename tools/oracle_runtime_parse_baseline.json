{"total": 181, "description": "oracle-text substring checks outside oracle_parser.py — baseline must equal actual count; both regressions (total > baseline) and stale baselines (total < baseline) fail CI"}
