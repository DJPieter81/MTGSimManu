{
  "total": 180,
  "description": "runtime oracle-text inspections in scoring / AI / bookkeeping code (AST data-flow detector; the parse-once and resolution-fallback layers are excluded \u2014 see check_oracle_runtime_parse.py). May only shrink: a new inspection fails CI (build a typed CardTemplate field), a typed-field migration lowers this in the same commit."
}
