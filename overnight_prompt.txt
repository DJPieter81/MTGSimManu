cd ~/MTGSimManu && git pull origin main

Run this command:
python3 iterate.py --check --n 5

Read the output. Pick the worst outlier. Run two traces:
python3 run_meta.py --verbose "DeckA" "DeckB" -s 90000
python3 run_meta.py --verbose "DeckA" "DeckB" -s 90100

Make ONE fix. Rules:
- Max 10 lines, 1 file
- ONLY these files: ai/ev_player.py, ai/board_eval.py, ai/evaluator.py, ai/strategy_profile.py, engine/sideboard_manager.py, decks/gameplans/*.json
- No card names in code. Use oracle text, tags, CMC, power/toughness.
- No magic numbers. Derive from game state: ratios, fractions, scaling.

Test: python3 iterate.py --matchup "DeckA" "DeckB" --n 10
If improved >=5pp with no regression: git add, commit, push.
If not: git checkout -- the file.

Do NOT touch any other file. Do NOT build guides, replays, HTML, or docs. Do NOT fix build_replay.py or card_effects.py or game_state.py or combat_manager.py. Only iterate WR fixes.

If the outlier is too hard after 2 traces, skip it and try the next outlier.

Check git log --oneline -5 first to see what previous launches already fixed.
